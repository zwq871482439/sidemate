

# Patch11 测试用例 — Chat Action + KB 多轮对话

> 测试日期：____年__月__日
> 测试环境：桌伴 Sidemate Patch11，Qwen3-8B + KB (bge-base-zh + bge-reranker)
> 测试文档：`test_cases/kb_docs/` 下 5 个中医养生文档

---

## 前置条件

- [x] 服务已启动（重启后端使新代码生效）
- [x] 8B 模型已加载
- [x] KB 模块已激活（设置页 → 知识库）
- [x] 上传 5 个测试文档到文库，等待全部"就绪"

存在问题：文档大小和字数显示待确认

✅**中医教你刮五指，帮你从头到脚疏.docx**🖼️

0.9KB · 1块 · 0.3K字 · 📄预览

显示只有0.9kb，和原文档大小差异较大

## 测试文档清单

| 编号 | 文件名 | 大小 | 特点 |
|------|--------|------|------|
| D1 | 中医教你刮五指，帮你从头到脚疏.docx | 42KB | 大文件，经络穴位专有名词密集 |
| D2 | 中医12个流派多样性和传承发展.docx | 18KB | 中等，历史流派信息，结构化内容 |
| D3 | 最好的健脑运动复杂环境走.docx | 22KB | 中等，运动+神经科学交叉内容 |
| D4 | 总结20个常见错误养生法，必看.docx | 13KB | 列表型内容（适合精确检索） |
| D5 | 最顶级的养生就是玩.docx | 14KB | 养生理念类（与 D4 主题重叠） |

---



## Part A：Chat Action 路由测试

> 在「💬对话」Tab 中测试，验证不同 action_mode 的路由行为

### A-01：默认 Chat 模式 — 普通对话

**操作**：选择「💬 对话」Action，输入：
```
你好，介绍一下你自己
```

**预期**：

- [ ] 正常回复（非 KB 检索）
- [ ] 无"📚 已检索知识库"提示
- [ ] 策略日志显示 greeting





---

### A-02：Chat 模式 — /code 策略覆盖

**操作**：选择「💬 对话」Action，输入：

```
/code 用 Python 写一个冒泡排序
```

**预期**：

- [ ] 显示"💻 编程策略（本次）"slash_hint
- [ ] 回复包含代码
- [ ] 策略日志显示 code



未通过！！

12:07:05

/code 用 Python 写一个冒泡排序


好的，我现在要帮用户提供一个Python的冒泡排序实现。首先得确认用户的需求是正确的，并且按照他们给定的规则来回答。

根据用户的指示：必须使用全中文题目和变量名，代码中的变量要用英文，不能在答案中包含多余的内容，直接给出解决方案即可。同时不需要提供额外的信息或者询问是否还有其他的问题，所以我要确保我的回复符合这些格式限制。

我需要用标准的英语命名法为变量起名字，比如数组可以叫做arr，然后循环控制变量i、j等。接下来就是编写具体的步骤了：



- 首先是定义函数bubble_sort(arr)，参数是一个列表。
- 然后进行外层循环遍历每个元素，内层则比较相邻两个数并交换位置。
- 为了优化性能，在每次完成一次轮询之后会检









---

### A-03：Chat 模式 — /kb 指令切换到 KB

**操作**：选择「💬 对话」Action，输入：
```
/kb 中医有哪些主要流派？
```

**预期**：
- [ ] 显示"📚 知识库模式（本次）"slash_hint
- [ ] 显示"📚 已检索知识库（N条）"mode_hint
- [ ] 回答基于 D2 内容，引用来源
- [ ] 策略走 KB 分支（kb_mode=True）

---

### A-04：Chat 模式 — /math 策略

**操作**：选择「💬 对话」Action，输入：
```
/math 解方程 2x + 5 = 13
```

**预期**：
- [ ] 显示"🔢 数学策略（本次）"
- [ ] 分步解题
- [ ] 策略日志显示 math

---

### A-05：Chat 模式 — /deep 深度分析

**操作**：选择「💬 对话」Action，输入：
```
/deep 分析一下有氧运动和无氧运动对大脑的不同影响
```

**预期**：
- [ ] 显示"🔍 深度分析策略（本次）"
- [ ] 多角度分析
- [ ] 策略日志显示 analysis

---

### A-06：Chat 模式 — /sum 摘要

**操作**：选择「💬 对话」Action，输入：
```
/sum 养生的核心理念是什么
```

**预期**：
- [ ] 显示"📋 摘要策略（本次）"
- [ ] 简洁提炼要点
- [ ] 策略日志显示 summarize

---

### A-07：Chat 模式 — /write 创意写作

**操作**：选择「💬 对话」Action，输入：
```
/write 写一首关于春天的小诗
```

**预期**：
- [ ] 显示"✍️ 创意写作策略（本次）"
- [ ] 有文采的创作内容
- [ ] 策略日志显示 creative

---

### A-08：Chat 模式 — 未知指令

**操作**：选择「💬 对话」Action，输入：
```
/xyz 这是指令测试
```

**预期**：
- [ ] 不识别 /xyz，原样处理
- [ ] 走 default 策略
- [ ] 正常回复

---

### A-09：Chat 模式 — 自动策略检测（code 关键词）

**操作**：选择「💬 对话」Action，输入：
```
帮我写一个 JavaScript 的防抖函数
```

**预期**：
- [ ] 不需要 /code 前缀也能识别为 code 策略
- [ ] 回复包含代码
- [ ] 策略日志显示 code

---

### A-10：Chat 模式 — 自动策略检测（math 关键词）

**操作**：选择「💬 对话」Action，输入：
```
计算 1+2+3+...+100 等于多少
```

**预期**：
- [ ] 自动识别为 math 策略
- [ ] 分步计算
- [ ] 策略日志显示 math

---

## Part B：KB Tab 问答测试

> 在「📚文库」Tab 中测试，验证 KB 独立端点

### B-01：精确匹配检索

**操作**：切换到📚文库 Tab，输入：
```
刮五指对应哪些脏腑？
```

**预期**：

- [x] 显示"🔍 正在检索知识库..."
- [ ] 回答包含五指对应脏腑（来自 D1）
- [ ] 引用来源标注正确
- [ ] 无 think 内容显示（或正确折叠）



未通过！！！

刮五指对应哪些脏腑？



📖 参考资料 3 条

[1] 中医教你刮五指，帮你从头到脚疏.docx ⬤ 高度相关

中医教你刮五指，帮你从头到脚疏通经络，让体内气血畅通！ 大拇指——脾经和肺经 作用：疏通经络，调畅体内气血，健脾和胃 表现：咳嗽，胸闷气喘，食欲不振，消化差，乏力没有精神等 食指——大肠经 作用：行气，疏通经络，调和肠胃 表现：手脚麻木僵硬，消化不好，口苦口臭，腹痛腹胀，便秘等 中指——心经和心包经 作用：促进血液循环，缓解疲劳，调节心脏功能 表现：心慌心悸，失眠多梦，神疲乏力，胸闷气短等 无名指

[2] 中医12个流派多样性和传承发展.docx §7.易水学派 ◉ 相关

§ 7.易水学派

7.易水学派 ◉ 脏腑病机研究 易水学派，由金代易州人张元素创立，其研究重心在于脏腑病机。张元素在该领域建立了系统的理论和方法，不仅推动了脏腑病症的诊断和治疗，为脾胃学派的发展奠定了基石。李杲，作为张元素的杰出弟子，继承并发展了易水学派的思想，成为了该学派的中流砥柱。 09

[3] 中医12个流派多样性和传承发展.docx §9.温补学派 ◉ 相关

§ 9.温补学派

9.温补学派 ◉ 脾肾研究 温补学派，这一流派在明代逐渐形成，其先







---

### B-02：语义理解检索（同义词）

**操作**：📚文库 Tab，输入：

```
手指和内脏有什么关系？
```

**预期**：
- [ ] 能匹配到 D1 内容（虽未说"刮五指"）
- [ ] 向量检索语义理解有效



未通过！！！

手指和内脏有什么关系？



📖 参考资料 2 条

[1] 中医教你刮五指，帮你从头到脚疏.docx ⬤ 高度相关

中医教你刮五指，帮你从头到脚疏通经络，让体内气血畅通！ 大拇指——脾经和肺经 作用：疏通经络，调畅体内气血，健脾和胃 表现：咳嗽，胸闷气喘，食欲不振，消化差，乏力没有精神等 食指——大肠经 作用：行气，疏通经络，调和肠胃 表现：手脚麻木僵硬，消化不好，口苦口臭，腹痛腹胀，便秘等 中指——心经和心包经 作用：促进血液循环，缓解疲劳，调节心脏功能 表现：心慌心悸，失眠多梦，神疲乏力，胸闷气短等 无名指

[2] 总结20个常见错误养生法，必看.docx §11.有些食物该洗不洗 ◉ 相关

§ 11.有些食物该洗不洗

11.有些食物该洗不洗 吃李子或草莓的时候，我们都会先清洗一下。但吃桔子、芒果等带皮食物的时候，很少有人把它们放进水冲洗。

问题：模型没有正常响应！！！



![image-20260527121242797](C:\Users\slow\AppData\Roaming\Typora\typora-user-images\image-20260527121242797.png)

---

### B-03：列表型内容精确提取

**操作**：📚文库 Tab，输入：
```
常见的错误养生方法有哪些？举3个例子
```

**预期**：
- [ ] 从 D4 中提取具体错误养生法
- [ ] 内容准确，不是编造
- [ ] 引用来源标注为 D4

---

### B-04：跨文档检索

**操作**：📚文库 Tab，输入：
```
运动对大脑有什么好处？
```

**预期**：
- [ ] 主要内容来自 D3（健脑运动）
- [ ] 可能辅助引用 D4 或 D5
- [ ] 多个来源引用标注

---

### B-05：超出范围的问题

**操作**：📚文库 Tab，输入：
```
量子力学的基本原理是什么？
```

**预期**：
- [ ] 明确回答"知识库中未找到"
- [ ] 不编造答案

---

### B-06：诱导性问题（反幻觉）

**操作**：📚文库 Tab，输入：
```
中医流派中，火神派是不是最重视针灸？
```

**预期**：
- [ ] 如果 D2 有相关内容，按文档纠正
- [ ] 不顺着错误前提编造
- [ ] 回答基于文档内容

---

### B-07：数字精确性验证

**操作**：📚文库 Tab，输入：
```
错误养生法一共列举了多少条？
```

**预期**：
- [ ] 回答数量与 D4 实际数量一致
- [ ] 不是模型估算

---

## Part C：Chat Tab → KB Action 多轮对话测试 ⭐

> 在「💬对话」Tab 中，选择「📚 文库」Action 按钮，测试从 Chat 发起的 KB 多轮对话

### C-01：首轮 KB 问答

**操作**：选择「📚 文库」Action，输入：
```
中医有哪些主要流派？
```

**预期**：
- [ ] 显示"📚 已检索知识库"提示
- [ ] 回答基于 D2 内容
- [ ] 无 think 泄露（正确折叠或无 think）
- [ ] 正文完整，不被截断

---

### C-02：追问（同主题深入）

**操作**：在 C-01 对话后继续输入：
```
这些流派中哪个最注重温阳？
```

**预期**：
- [ ] 再次检索知识库
- [ ] 回答基于 D2 中火神派/温阳派内容
- [ ] 能关联上下文（知道"这些流派"指什么）

---

### C-03：跨主题切换

**操作**：在 C-02 后切换问题：
```
刮大拇指有什么好处？
```

**预期**：
- [ ] 检索到 D1 内容
- [ ] 回答准确
- [ ] 不会串到之前的流派话题

---

### C-04：知识库无答案 fallback

**操作**：选择「📚 文库」Action，输入：
```
Python怎么安装？
```

**预期**：
- [ ] 显示"知识库中未找到相关内容，将使用模型直接回答"
- [ ] 走普通对话路径
- [ ] 正常回答 Python 安装方法

---

### C-05：多轮连续 KB 对话

**操作**：选择「📚 文库」Action，依次输入：
1. `错误养生法有哪些？` → 等回答
2. `第一条约提到了什么？` → 等回答
3. `还有其他相关的吗？` → 等回答

**预期**：
- [ ] 每次都正确触发 KB 检索
- [ ] 回答不串题
- [ ] 多轮上下文关联正确（第2问能理解"第一条"）
- [ ] 无 think 泄露，正文完整

---

### C-06：KB → Chat 模式切换

**操作**：
1. 先在「📚 文库」Action 下问：`运动对大脑有什么好处？`
2. 切换回「💬 对话」Action，问：`推荐几个适合办公室的简单运动`

**预期**：
- [ ] 第1问走 KB 检索，回答基于 D3
- [ ] 第2问走普通对话，不检索 KB
- [ ] 模式切换干净，无残留

---

## Part D：策略 × Action 组合测试

> 验证 /xx 指令与 Action 按钮的组合行为

### D-01：/kb 指令在 Chat Action 下

**操作**：「💬 对话」Action，输入：
```
/kb 刮五指分别对应什么？
```

**预期**：
- [ ] /kb 切换 action 为 kb
- [ ] 触发 KB 检索
- [ ] 回答基于 D1

---

### D-02：/code 指令在 KB Action 下

**操作**：「📚 文库」Action，输入：
```
/code 写一个Python函数计算BMI
```

**预期**：
- [ ] /code 只覆盖 strategy 不改 action
- [ ] action 仍为 kb → 触发 KB 检索
- [ ] 回答可能与 KB 内容混合（预期行为：KB 检索"写一个Python函数计算BMI"→ 无结果 → fallback 普通对话 → code 策略）

---

### D-03：连续 /xx 指令

**操作**：依次输入：
1. `/math 3的5次方等于多少` → 等回答
2. `/code 写个函数计算任意数的任意次方` → 等回答
3. `这个函数的时间复杂度是多少` → 等回答（无前缀）

**预期**：
- [ ] 第1问走 math 策略
- [ ] 第2问走 code 策略
- [ ] 第3问无前缀 → 自动检测（大概率 code）
- [ ] 每次策略正确切换

---

## Part E：边界与异常测试

### E-01：空消息

**操作**：不输入任何内容，直接点击发送

**预期**：
- [ ] 不崩溃
- [ ] 输入框抖动或提示"请输入内容"

---

### E-02：超长消息

**操作**：粘贴一段 2000 字的文本发送

**预期**：
- [ ] 正常处理
- [ ] 不超时、不截断

---

### E-03：KB 未激活时选📚文库 Action

**操作**：不激活 KB 模块，选择「📚 文库」Action 发消息

**预期**：
- [ ] 提示知识库未激活
- [ ] 或 fallback 到普通对话

---

### E-04：快速连续发送

**操作**：快速连续发送 3 条消息：
```
你好
刮五指有什么好处？
错误养生法有哪些？
```

**预期**：
- [ ] 每条都有回复
- [ ] 不崩溃、不卡死
- [ ] 不会串回答

---

### E-05：Think 泄露专项检测

**操作**：在「📚 文库」Action 下，连续问 5 个不同问题，检查每条回复：
```
中医流派有哪些？
刮五指的好处？
错误养生法？
养生就是玩什么意思？
健脑运动是什么？
```

**预期**：
- [ ] 每条回复正文完整（非空）
- [ ] 思考内容正确折叠（如果有）
- [ ] 不出现"与其所属"等截断片段
- [ ] 不出现 think 标签泄露到正文

---

## 测试结果汇总

| 用例 | 结果 | 备注 |
|------|------|------|
| A-01 | ⬜ | |
| A-02 | ⬜ | |
| A-03 | ⬜ | |
| A-04 | ⬜ | |
| A-05 | ⬜ | |
| A-06 | ⬜ | |
| A-07 | ⬜ | |
| A-08 | ⬜ | |
| A-09 | ⬜ | |
| A-10 | ⬜ | |
| B-01 | ⬜ | |
| B-02 | ⬜ | |
| B-03 | ⬜ | |
| B-04 | ⬜ | |
| B-05 | ⬜ | |
| B-06 | ⬜ | |
| B-07 | ⬜ | |
| C-01 | ⬜ | |
| C-02 | ⬜ | |
| C-03 | ⬜ | |
| C-04 | ⬜ | |
| C-05 | ⬜ | |
| C-06 | ⬜ | |
| D-01 | ⬜ | |
| D-02 | ⬜ | |
| D-03 | ⬜ | |
| E-01 | ⬜ | |
| E-02 | ⬜ | |
| E-03 | ⬜ | |
| E-04 | ⬜ | |
| E-05 | ⬜ | |

---

## 推荐测试顺序

1. **A-01 ~ A-10**：Chat Action 基本路由（先确保普通对话正常）
2. **C-01 ~ C-06**：Chat → KB 多轮对话（⭐ 重点！验证新修复的 KB 分支）
3. **B-01 ~ B-07**：KB Tab 独立问答（对比 Chat KB 和 KB Tab 的行为差异）
4. **D-01 ~ D-03**：策略组合（/xx 指令 + Action 按钮）
5. **E-01 ~ E-05**：边界异常（兜底）
