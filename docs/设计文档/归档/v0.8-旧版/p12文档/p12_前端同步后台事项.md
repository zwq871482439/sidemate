# Patch12 前端向后台同步事项

> 日期：2026-05-29 | 对象：后端团队  
> 背景：前端完成全面 UX 审查及 Patch12 升级，以下为需要后台配合的事项

---

## 一、术语统一修改（必须）

背景：前端已全局统一 "知识库→文库"、"语音转写→纪要"、"主模型→AI模型"。以下为用户可见文案中仍未修改的残留。

### 后端文件

**`action_registry.py` 第13行**（影响：对话Tab Action按钮栏 + 设置→Action管理tag）

```python
# 当前
"kb": {"label": "📚", "title": "检索知识库", "placeholder": "输入问题，自动检索知识库…", "tag": "知识库扩展"},

# 改为
"kb": {"label": "📚", "title": "检索文库", "placeholder": "输入问题，自动检索文库…", "tag": "文库扩展"},
```

### 前端文件（如后端团队也负责前端文件，请一并修改）

| # | 文件 | 行号 | 当前 | 改为 |
|---|------|------|------|------|
| 1 | `static/js/settings.js` | 284 | `知识库（文档管理 + 语义检索 + 问答）` | `文库（文档管理 + 语义检索 + 问答）` |
| 2 | `static/js/settings.js` | 512 | `安装模型、知识库、语音等扩展` | `安装模型、文库、纪要等扩展` |
| 3 | `static/js/settings.js` | 517 | `knowledge:'知识库', whisper:'语音'` | `knowledge:'文库', whisper:'纪要'` |
| 4 | `index.html` | 136 | `请先加载主模型` | `请先加载AI模型` |
| 5 | `index.html` | 505 | `语音转写更快` | `纪要转写更快` |
| 6 | `static/js/chat.js` | 199 | `语音转写能力` | `纪要转写能力` |
| 7 | `static/js/minutes.js` | 208 | `语音转写功能` | `纪要功能` |
| 8 | `static/js/minutes.js` | 922 | `已导入知识库` | `已导入文库` |
| 9 | `static/js/core/errors.js` | 24 | `知识库未就绪 / 请先安装知识库模块` | `文库未就绪 / 请先安装文库模块` |

---

## 二、API 契约核对问题

### 待确认：Pipeline 端点

前端 `chat.js:1200-1226` 调用了以下 4 个端点，但 **未出现在 p12_API_CONTRACT.md 中**：

| 方法 | 端点 | 用途 |
|------|------|------|
| POST | `/api/chat/pipeline/{id}/approve` | 批准确认 |
| POST | `/api/chat/pipeline/{id}/pause` | 暂停 |
| POST | `/api/chat/pipeline/{id}/resume` | 恢复 |
| POST | `/api/chat/pipeline/{id}/cancel` | 取消 |

请确认：**仍在服役** → 请补入 API 契约文档；**已废弃** → 前端将移除调用。

### 建议：心跳改用 /api/health

契约新增了 `GET /api/health`，当前前端心跳仍使用 `/api/status`（返回完整模型状态 JSON）。建议切换为更轻量的 `/api/health`，减少不必要的序列化开销。

### 参考：新端点暂未使用

以下 P12 新增端点前端暂未调用，如有配套需求请告知：

| 端点 | 说明 |
|------|------|
| `GET /api/workspace` | 沙盒文件列表 |
| `GET /api/workspace/{path}` | 沙盒文件下载 |
| `POST /api/kb/search` | 文库纯检索（不调 LLM） |
| `POST /api/kb/import_text` | 直接导入文本到文库 |

---

## 三、前端已完成的改动速览（供参考）

以下改动后端无需操作，仅作信息同步：

| 类别 | 改动量 | 说明 |
|------|:--:|------|
| 按钮系统统一 | 5套→1套 | `.btn` 令牌系统，兼容旧类名 |
| alert/confirm 替换 | 59处 | 全部替换为自定义弹窗 + Toast |
| CSS 颜色变量化 | 30+处 | 硬编码色彩全部抽为 CSS 变量 |
| 暗色模式主色修复 | 1处 | 保持蓝色色相不变 |
| 首次使用引导 | 新增 | 三步流程 + `_model_ever_loaded` 标记 |
| 增量渲染优化 | 2处 | `renderMessages` + `loadMinutesHistory` |
| z-index 理顺 | 4处 | 100→250→350→500 均匀分布 |
