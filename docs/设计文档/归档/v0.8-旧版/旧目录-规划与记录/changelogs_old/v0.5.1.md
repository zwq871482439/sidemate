# v0.5.1 变更记录 (2026-04-30)

## 里程碑：v0.5 上线后 Bug 修复

### Bug 修复（4 项）

| # | Bug | 影响 | 修复 |
|---|-----|------|------|
| 1 | Session 删除未删 JSON 文件 | 磁盘上残留废弃文件 | 后端 `os.remove` + `_current_chat_file` 重置；前端先清空再请求 |
| 2 | 未加载模型发消息无提示 | 用户发消息后无反应 | `sendMessage()` 前检查 `modelTag` class，未加载弹 alert |
| 3 | 模型管理 UI 杂乱 | OCR 显示在列表/按钮分散/状态不清 | OCR 不显示；NPU/GPU 灰色标签；加载/卸载/切换合并为一个按钮 |
| 4 | System prompt 过度思考 | 模型回复冗长 | 精简为"用中文简洁回答。直接给出答案，不要重复问题。" |

### UI 改进
- NPU/GPU 灰色标签（未加载时 CPU 不显示）
- 预留离线/在线红绿标签（v0.6 实现在线模式时启用）
- 加载/卸载/切换合并为单一按钮
