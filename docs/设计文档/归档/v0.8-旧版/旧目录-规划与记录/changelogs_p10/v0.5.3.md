# v0.5.3 变更记录 (2026-04-30)

## 改动

### 第一轮：模型管理修复 + 标签优化
1. **重新扫描按钮**：后端 `/api/rescan` 增量对比（新增/移除），前端 `rescanModels()` + 加载动画
2. **设备标签**：NPU→"NPU模式"、GPU→"GPU模式"、未加载→"--"
3. **在线标签**：有模型→"正在使用本地AI模型"（绿色 .on）、无→"--"（灰色 .off）

### 第二轮：6 项修复
1. **Session 删除链修复**：后端 `_new_chat_file()` 不再在删除时调用，只重置指针
2. **刷新加载动画**：按钮禁用+"刷新中..."文字+状态文本提示
3. **立即思考指示器**：`sendMessage()` 后立刻 `appendStreamingMsg('', '', 0, null, true)`
4. **思考内容全展示**：去掉 `max-height:150px;overflow:hidden`
5. **跳到底部按钮**：浮动按钮，距底部 >120px 显示
6. **DOMContentLoaded 合并**：OCR 拖拽 + 滚动检测合并为一个监听器

## 文件变更清单
| 文件 | 变更类型 |
|------|----------|
| `server.py` | 修改（rescan API + session 删除修复 + 版本号） |
| `index.html` | 修改（标签/动画/思考/跳底/合并监听） |
