# Patch3 发布前测试清单

## 静态检查结果 ✅

| 检查项 | 状态 |
|--------|------|
| 全部 .py 文件语法 | ✅ 通过 |
| 全部 13 个 .js 文件语法 | ✅ 通过 |
| DOM id 交叉引用（sendBtn/stopBtn/kbInput/messages/modelTag 等） | ✅ 一致 |
| window 全局函数导出完整性 | ✅ 通过 |
| kbUninstallModule 引用残留 | ✅ 0（已彻底删除） |
| iconSvg 内 emoji 残留（innerHTML 路径） | ✅ 0（全部迁移） |
| tooltip/title 中无 iconSvg 误用 | ✅ 通过 |

---

## 🚀 功能测试（模拟用户启动 Sidemate.exe）

### 阶段 1：启动与初始化
- [x] 双击 `Sidemate.exe` 启动
- [x] Ollama 进程自动拉起（Vulkan 模式优先）
- [x] FastAPI 后端启动成功（控制台无报错）
- [x] 浏览器自动打开，页面加载无 JS 错误（F12 Console）
- [x] **空状态引导**：Chat Tab 显示 think 图标 + "开始对话吧" + "输入问题或上传文件开始使用"
- [x] **模式标签**：页面上方显示 "本地" 或 "本地 Qwen3.5-4B" 标签
- [x] **上下文环形指示器**：显示在工具栏区域，初始应为低占用颜色

### 阶段 2：Chat 基础对话
- [x] 输入 "你好"，点击发送或按 Enter
- [x] **Send/Stop 按钮**：发送后 SendBtn 隐藏，StopBtn 显示
- [x] **思考指示器**：显示 think SVG 图标 + "正在思考..." + 跳动圆点
- [x] **流式输出**：文字逐字出现，不卡顿
- [x] **SVG 图标渲染**：所有状态图标（思考、搜索、完成等）显示为 SVG 线条图标，不是 emoji
- [x] **Tab 键**：在输入框按 Tab，光标不跳出（不跳到隐藏 tab）
- [x] 生成完成后 **StopBtn 隐藏，SendBtn 恢复**
- [x] **DOMPurify 净化**：最终渲染的消息无 XSS 风险（输入 `<script>alert(1)</script>` 应被过滤）

### 阶段 3：Dark Mode
- [x] 点击设置中深色模式开关
- [x] **全局颜色跟随**：背景、文字、侧边栏全部切换
- [x] **Sidebar active item**：选中的会话项背景不崩溃，使用 `var(--bg-tertiary)`
- [x] **SVG 图标颜色**：所有图标跟随文字颜色（currentColor）
- [x] **上下文环形指示器**：颜色跟随主题

### 阶段 4：文库 Tab（KB）
- [x] 切换到文库 Tab
- [x] 文档列表加载正常
- [x] **KB 提问**：输入问题后点"提问"
- [x] **KB 停止按钮**：生成中，提问按钮变为停止 SVG 图标；点击可中断生成
- [x] 中断后按钮恢复为提问图标
- [x] **KB 错误处理**：无模型时显示 warn 图标 + 提示文字（不是 emoji）
- [x] **文档状态图标**：文档列表中各状态（完成/错误/处理中/暂停）显示对应 SVG 图标

### 阶段 5：会话管理
- [x] 创建新对话
- [x] **侧边栏切换**：不同会话间切换正常
- [x] **删除对话**：删除后侧边栏更新，无多余 Toast
- [x] **重命名对话**：双击可编辑名称

### 阶段 6：心跳与连接
- [x] **Heartbeat 正常**：页面运行 5 分钟以上无异常断连
- [x] **Tab 切换**：切换到其他浏览器 Tab，心跳暂停；切回来恢复
- [x] **离线检测**：手动停止后端 → 显示离线横幅 → 点击重试

### 阶段 7：边界场景
- [ ] **上下文告警**：连续对话约 10 轮后，环形指示器变黄 → 变红
- [ ] **上下文 Tooltip**：鼠标悬停环形指示器，显示 token 使用信息（纯文字，无乱码）
- [ ] **空对话**：清空所有消息后，空状态引导正常显示

---

## ⚠️ 已知注意事项
1. **后端 /api/kb/uninstall-module 路由保留**：前端已删除卸载按钮，但后端路由仍在（设置页扩展管理使用）
2. **console.log 中的 emoji**：settings.js 的调试日志保留了 ⚠️ emoji（仅控制台显示，不影响 UI）
3. **tooltip 纯文本**：上下文 tooltip 中使用 `[!]` 替代原 ⚠️（因为 title 属性不支持 HTML）

---

## 📋 发版判定标准

| 条件 | 要求 |
|------|------|
| 静态检查 | ✅ 全部通过 |
| 功能测试 | 全部 ✅ 或 非关键项有已知说明 |
| 回归风险 | Chat/KB 基础对话无异常 |
| 安全检查 | DOMPurify 净化生效 |

**全部通过 → 可发版 🚀**
