# 2026-06-24

## KB v2 Indigo 设计迭代（已 commit f46ef6c）
- **追问按钮换行**: `.kb-dash-ask` 改为 `overflow-wrap:break-word; word-break:break-word`
- **双工具栏 sticky**: `.kb-toolbar` (top:0) + `.kb-batch-toolbar` (top:0, margin-bottom:-36px 浮层)
- **批量私密对话框文案**: "设为私密后，云端模型将无法读取该文档内容。" / "取消私密后，文档将恢复对云端模型可见，所有已有令牌将被撤销。"（kb-batch.js）

## 原型 vs 实现对比
- v2 Indigo 重构已完成 15 项核心组件（渐变顶条、环形图、三重联动、pinned 卡片等）
- 文档卡片分类徽章：用户决定不加
- 侧栏色点/环形图扇区/环形图例 三处均统一 `_DONUT_COLORS` 调色板

## 令牌机制发现
- `access_token.py`: full/search/none 三级令牌，仅存内存
- Pipeline 层未传递 `accessible_doc_ids`，私密文档对话检索实际未过滤
- 前端 `kbGenerateToken()` 用原生 prompt() 弹窗
