# Sidemate v0.9 Patch 5 - 依赖版本锁定
# 安装: pip install -r requirements.txt
# 更新日期: 2026-06-20
# Python: 3.14 (Windows, 嵌入式)

# ===== 核心运行时 =====
fastapi==0.136.3
uvicorn==0.48.0
sse-starlette==3.4.4
starlette==1.2.1
python-multipart==0.0.30
pydantic==2.13.4
pydantic-settings==2.14.0
pydantic_core==2.46.4

# ===== Ollama HTTP 客户端 =====
httpx>=0.28.0

# ===== 搜索引擎（TLS 指纹伪装，可选）=====
# F10: curl_cffi 装了自动启用，绕过部分网站反爬；不装则用 httpx fallback（无伪装）
curl_cffi>=0.15.0

# ===== 文件上传（流式写入，避免大文件全量读入内存）=====
# P6: aiofiles 支持知识库上传时边收边写，缺失则 fallback 到全量读入（内存占用高）
aiofiles>=24.1.0

# ===== NLP / 嵌入 =====
sentence-transformers==5.5.1
transformers==5.9.0
tokenizers==0.22.2
huggingface_hub==1.17.0
safetensors==0.7.0
accelerate==1.13.0
scikit-learn==1.8.0
scipy==1.17.1

# ===== 知识库依赖（扩展包）=====
# Patch5 T03: FlagEmbedding 用于 bge-m3 dense+sparse 编码
FlagEmbedding>=1.3.0
# P6 清理: rank-bm25/jieba/modelscope/magika 已移除（无引用）
faiss-cpu>=1.7.0

# ===== 云端 AI =====
openai>=1.0.0

# ===== 语音转写（扩展包）=====
# 2026-06-28 许可证审计：faster-whisper (MIT) 之前由 Whisper 扩展包提供，
# 当前 0.9.6 纪要模块已归档（server/archive/recorder_pkg/），不进入用户安装包。
# 此处不声明 faster-whisper / ctranslate2，避免误导。

# ===== PDF/文档 =====
pdfplumber==0.11.9
pypdf==6.10.2
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
mammoth==1.11.0
# 0.10.1 M1-E：create_ppt 真 PPT（SVG 单一中间表示 → native PPTX，
# vendor ppt-master svg_to_pptx 链，MIT 署名见 server/vendor/ppt_master/）
skia-pathops>=0.8
uharfbuzz>=0.39
XlsxWriter>=3.2
# Patch5 B2: EPUB 解析（文件类型扩展）— P6 改用 zipfile(标准库) + BeautifulSoup(MIT)，
# 彻底去除 EbookLib(AGPLv3+) 依赖。
# Patch5 B2: RTF 解析（文件类型扩展）
striprtf>=0.0.26

# ===== 工具 =====
numpy==2.4.6
pandas==3.0.0
pillow==12.2.0
psutil==7.2.2
beautifulsoup4==4.14.3
lxml==6.1.1
readability-lxml>=0.4.0
Markdown==3.10.2
colorama==0.4.6
tqdm==4.67.3
pywin32==311
